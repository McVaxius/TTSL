Start a clean TTSL Python server session.
   - use this path
   - `python -m pip install -r server\requirements.txt`
For example if your network ip is 192.168.0.94  . you can test 127.0.0.1 if its all clients on localmachine
   - `python server\ttsl_server.py --host 192.168.0.94 --port 6942`
